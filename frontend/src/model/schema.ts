import { appSchema, tableSchema } from '@nozbe/watermelondb';

// Initial schema - will be populated in Phase 2
export const schema = appSchema({
  version: 1,
  tables: [
    // Tables will be added in Phase 2 (T009)
  ],
});
